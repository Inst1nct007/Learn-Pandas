# Important properties and methods

info(), shape, ndim, value_counts(), sort_index(), columns, index, isin(), 

1. WAP to create a dataframe

2. WAP to print the shape of the dataframe

3. WAP to print information about the df using info()

4. WAP to set the max columns to 50      pd.set_option('display.max_columns', 50)

5. WAP to print only the first 10 rows using head()

6. WAP to print only the last 10 rows using the tail()

7. WAP to print a row using iloc[]

8. WAP to set the index of a df using set_index()

9. WAP to set the index of a df using pd.read_csv(index_col='')

10. WAP to print a row using loc[]

11. WAP to sort index of a df in asc & desc using sort_index(ascending=, inplace=)

12. WAP to filter a df

#TIP In Pandas `AND` is represented as `&`. `OR` is represented as `|`. `NOT` is represented as `~`.
